<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Contained</title>
        <item>
            <title>1.0.0-nightly.151+0e3e14f</title>
            <pubDate>Sat, 18 Jul 2026 23:50:50 +0000</pubDate>
            <sparkle:version>151</sparkle:version>
            <sparkle:shortVersionString>1.0.0-nightly.151+0e3e14f</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
            <sparkle:hardwareRequirements>arm64</sparkle:hardwareRequirements>
            <description><![CDATA[<h2>Changes Since Last Nightly</h2>
<h3>Changed</h3>
<ul>
<li>Replaced avoidable AppKit panels, pasteboard writes, haptics, and WebView bridges with SwiftUI-native file, copy, feedback, and WebKit surfaces while keeping AppKit isolated to platform wrappers, SwiftTerm hosting, and material/vibrancy support.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Bundle the generated current release notes artifact into local app builds so in-app What's New matches the CI/Appcast release-note source.</li>
<li>Harden container recreate after stale runtime snapshots and reduce repeated optional-runtime endpoint errors in Activity.</li>
<li>Validate replacement and rollback recipes before destructive container edits, automatically restore the original after replacement failure, and retain its recipe when restoration also fails.</li>
<li>Link Core fixtures only into the native Xcode test target and run that suite in PR CI so the Xcode and SwiftPM package graphs cannot drift silently.</li>
<li>Keep raw CLI commands, stderr, paths, environment values, and credentials out of persistent Activity and public Console diagnostics while retaining runtime detail in immediate errors.</li>
</ul>
<h3>Runtime & Images</h3>
<ul>
<li>Added dormant Docker CLI adapter groundwork in `ContainedCore`, with Docker command builders, decoders, create/Compose translation, runtime descriptors, CLI discovery, and direct adapter tests kept out of the default app registry.</li>
<li>Kept Docker disabled in the default runtime registry so Contained discovers, probes, and offers Apple `container` only until a Docker provider model is chosen.</li>
<li>Added runtime-scoped resource routing and image/tag models so future runtimes can aggregate containers and expose runtime-specific local image availability through each resource's owning runtime.</li>
<li>Added runtime-scoped settings records; Apple service, kernel, and DNS controls remain Apple-only while dormant Docker path/endpoint UI stays hidden by default.</li>
<li>Kept V1 image storage runtime-owned while centralizing registry search, remote digest/update metadata, and normalized tag grouping across runtimes.</li>
<li>Scoped local image update comparisons to each runtime-owned tag so one runtime can be current while another has an update available for the same image reference.</li>
<li>Removed implicit runtime fallbacks so create, pull, build, load, push, registry, network, volume, logs, stats, terminal, and migration actions route through an explicit runtime or an existing resource owner.</li>
<li>Split registered runtime clients from ready runtime endpoints while keeping Apple-only service controls explicitly Apple-scoped.</li>
<li>Hardened the Core runtime boundary with module-driven Apple container and Docker adapters, runtime-keyed CLI overrides/readiness, runtime-owned schema profiles, and static checks preventing concrete runtime behavior from leaking out of `Runtimes/**`.</li>
<li>Moved the built-in module registry into shared `Runtime` infrastructure, kept `Runtimes/**` for concrete adapters only, and made canonical schema fields runtime-neutral while adapter profiles own support state and tips.</li>
<li>Tightened the runtime cleanup pass by splitting Core runtime/Compose helpers, surfacing partial inventory failures, removing app-side Apple defaults from unowned flows, and recording typed app-database failures instead of crashing on corrupt records.</li>
<li>Replaced alert-based runtime picking for no-context Compose/image archive imports with an in-app runtime selection sheet that preselects only when one compatible runtime is available.</li>
<li>Moved migration visibility and runtime move progress into the app database/Core migration flow, retaining disappeared resources only when they carry Contained-owned value.</li>
<li>Fixed the container-card morph regression by keying measured card frames and expanded overlays by runtime-scoped container IDs.</li>
</ul>
<h3>Architecture</h3>
<ul>
<li>Split Contained into standalone local packages: `ContainedCore` for backend orchestration, `ContainedUI` for reusable visual primitives, `ContainedUX` for interaction infrastructure, and `ContainedApp` for app-owned SwiftUI, settings, persistence, localization, and feature policy.</li>
<li>Added a checked-in Xcode workspace and native macOS app target that links the shared `ContainedApp` package product while keeping SwiftPM as the CI, release, signing, packaging, and appcast source of truth.</li>
<li>Kept deterministic preview/test samples in the separate `ContainedCoreFixtures` product so normal app and distributable builds do not link fixture data.</li>
<li>Reorganized docs into app, feature, architecture, development, release, package README, DocC, and wiki-map ownership areas.</li>
</ul>
<h3>Runtime & Creation</h3>
<ul>
<li>Consolidated runtime contracts, the Core orchestrator, the Apple `container` adapter, command previews, typed runtime methods, typed stats streams, Compose translation, image defaults, and runtime-neutral schema/import models inside `ContainedCore`.</li>
<li>Added open-ended runtime identifiers and runtime-scoped resource routing so future Docker, Podman, Lima-backed, remote, or other adapters can plug into Core without becoming app-side switches.</li>
<li>Replaced the app-owned Run/Edit request shape with Core-published schema documents that carry generic field paths, Apple/Docker/Compose source aliases, runtime-specific tips, disabled-field explanations, provenance, schema-conformance migration, and Apple `--os`/`--arch` support.</li>
<li>Routed terminal exec, service lifecycle actions, package errors, and reusable-package copy through Core/app-owned presentation boundaries.</li>
</ul>
<h3>UI & Performance</h3>
<ul>
<li>Extracted Liquid Glass cards, panels, controls, toolbar chrome, command previews, charts, console surfaces, badges, keycaps, status indicators, tint controls, and contextual tokens into `ContainedUI`.</li>
<li>Moved toolbar safe-area measurement, morph geometry, panel placement, and single-surface expansion infrastructure into `ContainedUX`.</li>
<li>Added colocated SwiftUI previews for design-system and UX primitives, removed the separate preview-only source directories, and documented that design previews live beside each element declaration.</li>
<li>Introduced package-internal `ContainedUI/Shared` helpers for repeated capsule, swatch, label, row, and surface-rendering anatomy while keeping app-facing calls on the public `UI.*` routes.</li>
<li>Centralized resource-card anatomy in `UI.Card.Scaffold`, with stable headers, sticky large widgets, body-hosted medium/small details, typed page controls, and packaged action/status routes.</li>
<li>Reworked Run/Edit storage into Storage Groups, where each group can hold multiple host/internal paths and can optionally be backed by one runtime volume with Contained-managed symlinks.</li>
<li>Reworked Run/Edit storage rows into one native form section per storage group, with labeled host folder, internal path, and explicit Read only/Read/Write access controls instead of a compact toggle.</li>
<li>Moved Run/Edit and Settings back onto native grouped SwiftUI forms and section footers, added a Run/Edit header page switcher, and moved Run/Save to the command-preview footer while preserving row-level info popovers.</li>
<li>Made Reveal CLI command previews render from each selected runtime descriptor instead of hardcoding the Apple `container` executable in the reusable UI package.</li>
<li>Kept grouped form viewports transparent so only native section backgrounds carry the form surface.</li>
<li>Added form label state colors so field-specific errors render red and changed-from-default rows render blue.</li>
<li>Reworked the Volumes page into vertical runtime-volume and host-path mount card groups, with runtime-scoped inventory keys so same-named volumes from future runtimes remain distinct.</li>
<li>Reduced idle UI churn with one app-wide low-priority stats stream, narrower per-container metric invalidation, lazy long panels, deferred heavyweight expanded-card pages, cached style/tag lookups, and coalesced image refreshes.</li>
<li>Improved metric rendering with chronological sparkline windows, fixed CPU/memory percentage scales, raw network/disk throughput shapes, configurable CPU/memory normalization, and clearer sub-1% CPU/memory readouts.</li>
</ul>
<h3>Release & Repository</h3>
<ul>
<li>Improved PR, release, CodeQL, Dependabot, support, security, issue-template, and release-note workflows so repository checks run for material source/script/workflow/package/test changes without rerunning for docs-only or appcast-only maintenance.</li>
<li>Capped PR, Nightly, Beta, and Stable macOS CI jobs with explicit 30-minute timeouts so wedged SwiftPM builds do not burn the six-hour GitHub Actions default.</li>
<li>Tightened release-note generation so stable releases ship full version notes, beta/nightly builds prepend channel changes, empty deltas are skipped deliberately, and release-script fixtures stay isolated unless a changes source is explicitly supplied.</li>
<li>Added repository validation for script strict mode and headers, workflow path filters, docs indexes, wiki mapping drift, stale names, package-boundary imports, app adapter internals, raw token use, empty source/docs folders, and bundled changelog sync.</li>
<li>Clarified that `CHANGELOG.md` remains curated version-level release notes while this consolidated change fragment carries PR/build-level deltas.</li>
</ul>
<h2>Full Release Notes</h2>
<h3>Highlights</h3>
<ul>
<li>First complete Contained release: a native macOS 26 SwiftUI control surface for Apple's `container` CLI.</li>
<li>Classic sidebar navigation is the default experience. The Liquid Glass toolbar, morph panels, command palette, Docker Hub search, Compose import, image build workspace, and keyboard shortcuts are available as explicit experimental features.</li>
<li>Contained stays CLI-first: privileged runtime work goes through Apple's `container` command, run/edit operations can reveal the generated `container ...` command, and decorative app metadata stays local to Contained.</li>
<li>Built-in Sparkle updates support Stable, Beta, and Nightly channels, with fresh pre-1.0 installs defaulting to Nightly so testers receive current builds.</li>
</ul>
<h3>App & Navigation</h3>
<ul>
<li>Full-page macOS app shell for Containers, Images, Build, Volumes, Networks, System, Templates, Activity, and Settings.</li>
<li>Optional menu-bar app with service health, unread Activity count, update checks, quick actions, and navigation back into the main window.</li>
<li>Shared presentation model for sidebar, menus, menu-bar actions, toolbar panels, sheets, and command-palette routes.</li>
<li>Toolbar-aware safe areas and page filters so experimental toolbar chrome does not cover classic sidebar content or expanded cards.</li>
<li>Keyboard shortcuts for common navigation, search, settings, creation, update, activity, and palette actions when the experimental gate is enabled.</li>
</ul>
<h3>Containers & Creation</h3>
<ul>
<li>Container grid with Liquid Glass cards, lifecycle actions, status, image, command, resource highlights, context menus, and full-card hit targets.</li>
<li>Grouping, sorting, and running-only filters for containers by network, volume, image, or flat list.</li>
<li>Expanded container tabs for Overview, Logs, Terminal, Stats, History, and Files, including live/follow logs, SwiftTerm terminal access, file browsing/copy workflows, and per-container metric history.</li>
<li>Shared Run/Edit form for new containers and recreate/edit flows, with validation, image pre-pull, inline errors, app-managed options, and exact CLI preview.</li>
<li>Compose import from paste, file picker, drag-and-drop, menu command, or command palette action, translating services into editable run forms instead of launching opaque stacks.</li>
<li>Compose translation for image, platform, command, entrypoint, ports, volumes, env files, network mode, restart policy, health checks, working directory, user, capabilities, DNS, tmpfs, and ulimits, with warnings for unsupported shapes.</li>
<li>Template storage for reusable run configurations using the same Run/Edit form as other creation paths.</li>
<li>App-managed restart policies and health checks for behavior Apple's `container` CLI does not provide natively.</li>
</ul>
<h3>Images, Resources & Registries</h3>
<ul>
<li>Local image and tag browsing with grouped references, run actions, history pages, tagging, pushing, saving archives, loading OCI `.tar` archives, pruning, and update status.</li>
<li>Image update checks based on local and remote digests, surfaced on image cards, container cards, toolbar panels, System, and command palette entries.</li>
<li>Manual and scheduled image update checks, including "check all", "pull all available image updates", and container-image specific sweeps.</li>
<li>Experimental Docker Hub search that can hand selected results into the run configuration flow.</li>
<li>Experimental image build workspace for Dockerfile/context builds with streamed BuildKit output.</li>
<li>Volume and network browsing, creation, deletion, prune actions, local styling, and mount/network-aware container grouping.</li>
<li>Registry credential management in Settings, including login/logout with passwords piped through `--password-stdin` rather than process arguments.</li>
</ul>
<h3>System, Settings & Activity</h3>
<ul>
<li>Bootstrap states for missing CLI, unsupported CLI version, stopped service, and ready service.</li>
<li>System page for runtime/service state, resource usage, background work, volume inventory, system logs, and runtime-scoped prune/service lifecycle actions.</li>
<li>Settings tabs for General, Appearance, Runtime, Registries, Experimental, Updates, and About.</li>
<li>Configurable appearance, tint, materials, card density, menu-bar behavior, CLI previews, info tips, logging, update cadence, image update cadence, and experimental feature gates.</li>
<li>Activity history for lifecycle, image, compose, system, registry, pull, build, watchdog, healthcheck, alert, and UI events, including unread state, filtering, copy/delete actions, and clear controls.</li>
<li>Versioned `.containedbackup` export/import for settings, personalization, health checks, templates, and activity history, with per-category selection and merge/replace behavior.</li>
<li>Rollback guard for local data created by newer app schemas, including export-before-reset and best-effort keep-readable-data paths.</li>
</ul>
<h3>Personalization & Accessibility</h3>
<ul>
<li>Local-only personalization for containers, image groups, image tags, and volumes, including nickname, icon, tint, background, graph/widget choices, and inheritance from image or app defaults.</li>
<li>CPU, memory, network, and disk widgets with configurable graph style, interpolation, tint, and normalization choices.</li>
<li>Accessibility-aware visual effects and motion handling, including Reduce Transparency and Reduce Motion support where Contained supplies custom glass or animation.</li>
</ul>
<h3>Reliability & Performance</h3>
<ul>
<li>Shared refresh coordination for service/list refreshes, lifecycle actions, image list refreshes, disk usage refreshes, and image-update checks to avoid unnecessary CLI process churn.</li>
<li>One app-wide low-priority stats stream for running containers, with narrower per-container metric invalidation and lazy long panels.</li>
<li>Chronological sparkline windows, fixed CPU/memory percentage scales, raw network/disk throughput shapes, configurable CPU/memory normalization, and clearer sub-1% readouts.</li>
<li>Deferred heavyweight expanded-card pages, cached style/tag lookups, and coalesced image refreshes so navigation and customization stay responsive.</li>
<li>Hardened terminal teardown so rapid card or tab switching cleans up `container exec --tty` children.</li>
<li>Compose import preserves user control by opening editable run forms and reporting unsupported values rather than silently guessing.</li>
<li>App-managed restart and health flows suppress user-initiated stops where appropriate and avoid uncontrolled restart loops.</li>
</ul>
<h3>Architecture & Release</h3>
<ul>
<li>Package-first SwiftPM layout with `ContainedCore` for backend orchestration, `ContainedUI` for visual primitives, `ContainedUX` for interaction infrastructure, `ContainedApp` for app-owned SwiftUI/persistence/localization policy, and a tiny `Contained` executable launcher.</li>
<li>`Core.Orchestrator` is the app-facing backend boundary; Core owns runtime descriptors, command previews, Apple `container` adapter internals, typed async runtime methods, Compose/image-default translation, and typed stats streams.</li>
<li>Core-published run/edit schema documents own editable runtime fields, schema conformance, validation, source aliases, disabled runtime explanations, command previews, and execution mapping.</li>
<li>`AppModel`, focused stores, `UIState`, and toolbar option enums centralize bootstrap, navigation, refresh coordination, image updates, settings, resource styles, filters, and fallback routing.</li>
<li>SwiftData-backed history records events, metric samples, and templates with bounded retention and backup/import support.</li>
<li>Checked-in Xcode workspace and native macOS app target build and run `Contained.app` directly while SwiftPM remains the CI, release, packaging, signing, notarization, and appcast source of truth.</li>
<li>Release scripts centralize version/build derivation, retain promoted nightly build numbers for beta/stable, compose channel-specific and full-version release notes, generate Sparkle appcast HTML, and keep the bundled changelog resource in sync.</li>
<li>Repository validation covers script strict mode and headers, workflow path filters, docs indexes, wiki mapping, package-boundary imports, app adapter internals, stale names, raw token use, empty source/docs folders, release-note composition, and bundled changelog sync.</li>
</ul>
<h3>Migration Notes</h3>
<ul>
<li>Saved local container, image, and volume styles are kept in local app storage.</li>
<li>Local settings, personalization, health checks, templates, and activity history can be exported before resetting data created by a newer app schema.</li>
<li>Activity events created before unread tracking are treated as unread on first launch.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/tdeverx/contained-app/releases/download/nightly-latest/Contained-1.0.0-nightly.151+0e3e14f.dmg" length="9187148" type="application/octet-stream" sparkle:edSignature="tQqKt8rMopVwRBYgmUvOj7FMomP7YgXQxddOICGN39AsZB3kJDqtXC0AbW+D89eAQJ0mUZ4JsbB4gVgtqCc1BA=="/>
        </item>
    </channel>
</rss>